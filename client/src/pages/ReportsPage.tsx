import { useState } from 'react';import { http } from '../api/http';
export default function ReportsPage(){const [r,s]=useState<any>(null); const load=()=>http.get('/reports',{params:{from:'2024-01-01',to:'2099-01-01'}}).then(x=>s(x.data.data)); return <div className='card p-3'><h4>Reports</h4><button className='btn btn-primary me-2' onClick={load}>Load</button><a className='btn btn-outline-primary me-2' href='http://localhost:4000/api/reports/csv'>Export CSV</a><a className='btn btn-outline-primary' href='http://localhost:4000/api/reports/pdf'>Export PDF</a>{r&&<pre>{JSON.stringify(r.forecast,null,2)}</pre>}</div>}
