import { useEffect, useState } from 'react';import { http } from '../api/http';import { LineChart,Line,XAxis,YAxis,CartesianGrid,Tooltip,ResponsiveContainer } from 'recharts';
export default function DashboardPage(){const [d,setD]=useState<any>({kpis:{},sales:[]}); useEffect(()=>{http.get('/dashboard').then(r=>setD(r.data.data));},[]); return <div><div className='row g-3'>{Object.entries(d.kpis||{}).map(([k,v])=><div className='col-md-3' key={k}><div className='card p-3'><small>{k}</small><h4>{String(v)}</h4></div></div>)}</div><div className='card p-3 mt-3'><h5>Sales Trend</h5><div style={{height:260}}><ResponsiveContainer><LineChart data={d.sales}><CartesianGrid strokeDasharray='3 3'/><XAxis dataKey='date'/><YAxis/><Tooltip/><Line dataKey='amount' stroke='#8884d8'/></LineChart></ResponsiveContainer></div></div></div>}
