import { useState } from 'react';import { useSearchParams } from 'react-router-dom';import { http } from '../api/http';
export default function ResetPasswordPage(){const [sp]=useSearchParams(); const [p,spw]=useState(''); const [m,sm]=useState(''); return <div className='card p-3'><h4>Reset Password</h4><input className='form-control mb-2' type='password' value={p} onChange={e=>spw(e.target.value)} placeholder='new password'/><button className='btn btn-primary' onClick={async()=>{await http.post('/auth/reset-password',{token:sp.get('token'),password:p}); sm('Password reset success');}}>Reset</button><div>{m}</div></div>}
